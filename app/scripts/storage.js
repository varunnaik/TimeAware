/**
 * Created by varun on 28/2/14.
 * Abstracts storage and provides storage interfaces allowing for easy porting
 * between web browsers.
 */
if (typeof Timeaware === 'undefined') Timeaware = {};
Timeaware.storage = {};
